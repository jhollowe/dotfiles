- defaults:
    link:
      create: true
      relink: true

- clean: ["~", "~/.config"]

- link:
    ~/.bashrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.ssh:
    ~/.shell:
    ~/.nanorc:

- shell:
    - [packages/install-packages.sh, Install packages for system]
